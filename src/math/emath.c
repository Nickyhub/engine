#include "emath.h"

#include <math.h>

f32 esin(f32 radians) {
    return sin(radians);
}

f32 ecos(f32 radians) {
    return cos(radians);
}

f32 etan(f32 radians) {
    return tan(radians);
}

f32 eabs(f32 value) {
    return abs(value);
}

f32 esqrt(f32 value) {
    return sqrt(value);
}